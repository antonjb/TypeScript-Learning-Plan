// Convert the following from inferred types to explicit types
() => {
    let hasPassed = true;

    let age = 50;

    let favouriteColour = 'orange';

    let values = [10, 30, 50];

    let version = ['typescript', 3];
};
