// Convert the following from inferred types to explicit types

() => {
    let hasPassed: boolean = true;

    let age: number = 50;

    let favouriteColour: string = 'orange';

    let values: number[] = [10, 30, 50];

    let version: [string, number] = ['typescript', 3];
};
